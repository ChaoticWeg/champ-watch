extends layouts/main

block styles

block scripts
	script(type='text/javascript').
		$(function () { $('.pathname').text(document.location.pathname); });

block content
	.row.text-center
		.col-xs-12.col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
			h1.page-header 404
			p Page not found: #[span.pathname]
